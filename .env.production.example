# Values prefixed with VITE_ are embedded at build time. Set the production
# API origin in the deployment environment; do not use a localhost value here.
VITE_API_URL=https://your-production-api-domain.com
